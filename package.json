{
  "devDependencies": {
    "vite-plugin-gh-pages": "^1.0.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vite-gh-pages"
  }
}
